services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: holodos
      POSTGRES_USER: holodos
      POSTGRES_PASSWORD: holodos
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
